Doc:

- Improvemenent - analytic request validation: stop returning all the supported analytic when user provide a wrond one

New feature:

# Handler Protection

new method is available registerHandlerProtectionScope(HandlerAccessibilityScope $accessible) in hour analytic handler redgister class. with this we we can

- define restruction on auth user only
- define restruction based on some conditions
- you can duplicate handler classes in auth and event based security scope, but no need to add them agin in the handler since the package will merge all of them before resolving the incoming analytic-type of the request
