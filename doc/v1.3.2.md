-   Data persistance - report table: Fix memory leak when rolling back the migration
-   support handler_type when creating log
-   fetch log data:

    -   when fetching tenant tags, groups and actions: you can now filter by given handler_types

-   prebuild analytic logs handlers:
    -   you can now filter by handler_types
