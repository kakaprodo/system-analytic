1. Support report logs in config file
    - this need to be enabled in the config first
    - then run migration
2. change logic to run migration: condition for not running migration content will be in the migration file and not in service provider
3. added analytic log gate: for creating logs
4. Analytic logs: added prebuilt log handlers handlers for:
    - piechart
    - bar chart
    - and card count
5. Analytic logs: added two endpoint to fetch unique - tenant's tags list - tenant's group list
   mea
6. Support the creation of many logs at the same time
